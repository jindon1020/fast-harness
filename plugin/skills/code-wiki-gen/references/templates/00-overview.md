# {Project Name} — System Overview

> Generated by code-wiki-gen | {date} | Commit: {commit_hash}

---

<!-- SECTION: system-type | sources: {entry_files} -->
## What This System Is

**Type:** {API server / CLI / worker / frontend / monorepo / ...}
**Primary language:** {language + version}
**Framework:** {framework + version}
**Entry point:** `{file:line}`

One-paragraph description of what this system does and who uses it.
<!-- END SECTION: system-type -->

---

<!-- SECTION: tech-stack | sources: {config_files} -->
## Tech Stack

| Layer | Technology | Config file |
|-------|-----------|-------------|
| Runtime | {e.g. Python 3.11} | `pyproject.toml` |
| Web framework | {e.g. FastAPI} | `src/main.py` |
| Database | {e.g. PostgreSQL via SQLAlchemy} | `src/db/session.py` |
| Cache | {e.g. Redis} | `src/cache/client.py` |
| Queue | {if any} | |
| Auth | {e.g. JWT via python-jose} | `common/auth/`
<!-- END SECTION: tech-stack -->

---

<!-- SECTION: module-map | sources: {top_level_dirs} -->
## Module Map

```
{project}/
├── {module-a}/     # {one-line purpose}
├── {module-b}/     # {one-line purpose}
├── common/         # shared capabilities (no business logic)
│   ├── auth/       # identity + permissions
│   ├── {x}/        # {purpose}
└── {infra}/        # db, cache, storage clients
```

Dependency direction (arrows = "depends on"):
```
{module-a} → common/auth
{module-a} → common/{x}
{module-b} → common/auth
{module-b} → common/{x}
```

No circular dependencies: {YES / NO — if NO, list them}
<!-- END SECTION: module-map -->

---

<!-- SECTION: entry-points | sources: {router_files} -->
## External Entry Points

| Method | Path prefix | Handled by | Auth required |
|--------|-------------|------------|---------------|
| * | `/drama/*` | `drama/router.py` | Yes — JWT |
| * | `/marketing/*` | `marketing/router.py` | Yes — JWT |
| * | `/internal/*` | `common/router.py` | ClusterIP only |

<!-- END SECTION: entry-points -->

---

<!-- SECTION: deployment | sources: {infra_files} -->
## Deployment Context

**Runtime:** {k8s / Docker / serverless / ...}
**Service discovery:** {k8s ClusterIP / consul / ...}
**Internal services reachable at:**
- `http://content-common-module` → common module
- `http://algo-manager` → model scheduling (internal only)

<!-- END SECTION: deployment -->