# Module Responsibilities

> Generated by code-wiki-gen | {date} | Commit: {commit_hash}

Each module has exactly one reason to exist. If you're unsure which module owns
a feature, check the "Owns" and "Does NOT own" sections below.

---

<!-- SECTION: {module-name} | sources: {module_files} -->
## {module-name}

**Responsibility:** {one sentence — what business problem does this solve?}

**Owns:**
- {capability 1} → `{file.py}:{function}`
- {capability 2} → `{file.py}:{function}`

**Does NOT own** (delegates to):
- User identity → `common/auth/`
- Asset storage → `common/asset/`
- {other boundary}

**Public interface** (what callers can use):
```
{METHOD} /{path}          → {handler file:function}
{METHOD} /{path}/{param}  → {handler file:function}
```

**Internal dependencies:**
```python
# This module imports from:
from common.auth import validate_token      # src/common/auth/middleware.py
from common.asset import AssetManager       # src/common/asset/service.py
```

**Key business rules:**
- {rule 1 — non-obvious logic that an AI needs to know}
- {rule 2}

**Data owned** (DB tables this module writes to):
- `{table_name}` — {what it stores}

> ⚠️ INFERRED: {anything uncertain — flag it so AI doesn't over-trust}
<!-- END SECTION: {module-name} -->

---
<!-- repeat for each module -->